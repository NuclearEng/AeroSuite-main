// Configuration settings for the AeroSuite client application

// API base URL, can be overridden by environment variables
export const API_BASE_URL = process.env.REACT_APP_API_BASE_URL || 'http://localhost:5000';

// Default pagination settings
export const DEFAULT_PAGE_SIZE = 10;
export const DEFAULT_PAGE = 1;

// Authentication configuration
export const AUTH_TOKEN_KEY = 'aerosuite_auth_token';
export const AUTH_REFRESH_TOKEN_KEY = 'aerosuite_refresh_token';
export const AUTH_USER_KEY = 'aerosuite_user';
export const AUTH_TOKEN_EXPIRY_BUFFER = 5 * 60 * 1000; // 5 minutes in milliseconds

// File upload settings
export const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB
export const ALLOWED_FILE_TYPES = [
  'image/jpeg',
  'image/png',
  'image/gif',
  'application/pdf',
  'application/msword',
  'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
  'application/vnd.ms-excel',
  'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'
];

// Date format settings
export const DEFAULT_DATE_FORMAT = 'MMM dd, yyyy';
export const DEFAULT_DATETIME_FORMAT = 'MMM dd, yyyy HH:mm';

// Theme settings
export const DEFAULT_THEME = 'light';

// Toast notifications settings
export const DEFAULT_TOAST_DURATION = 5000; // 5 seconds

// Cache settings
export const DEFAULT_CACHE_DURATION = 5 * 60 * 1000; // 5 minutes in milliseconds 