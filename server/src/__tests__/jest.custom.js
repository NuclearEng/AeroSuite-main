module.exports = {
  verbose: true,
  testEnvironment: 'node',
  testMatch: ['**/?(*.)+(spec|test).js'],
  collectCoverage: true,
  coverageDirectory: '<rootDir>/coverage',
  reporters: ['default']
}; 